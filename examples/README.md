Examples folder

This folder is reserved for additional example programs and usage notes. The main demo is at `src/main.cu` and builds to `bin/vector_add` by default.

To run the demo after building:

```bash
./bin/vector_add
```
